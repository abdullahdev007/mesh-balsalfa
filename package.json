{
  "name": "mesh-balsalfa",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "kill-port 3000 && kill-port 5000 && turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.4.0",
    "typescript": "5.7.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@11.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@fontsource/rubik": "^5.1.1",
    "web": "^0.1.0",
    "workspace": "^0.0.1-preview.2"
  }
}
